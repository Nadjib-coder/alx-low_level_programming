#include "main.h"

/**
 * div - a function that mod 2 integers
 * @a: the first integer
 * @b: the second integer
 * Return: The mod of 2 integers.
 */
int mod(int a, int b)
{
        return (a % b);
}


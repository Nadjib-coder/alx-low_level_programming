#include "main.h"

/**
 * add - a function that add 2 integers.
 * @a: the first integer.
 * @b: the second integer.
 * Return: the sum of the 2 integers.
 */
int add(int a, int b)
{
	return (a + b);
}


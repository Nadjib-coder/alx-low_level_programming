#include "main.h"

/**
 * mul - a function that multiplicate 2 integers
 * @a: the first integer
 * @b: the second integer
 * Return: the multiplication of the 2 integers.
 */
int mul(int a, int b)
{
	return (a * b);
}


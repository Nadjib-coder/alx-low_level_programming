#include "main.h"

/**
 * sub - a function that subtract 2 integers
 * @a: the first integer.
 * @b: the second integer.
 * Return: the sub of the 2 integers.
 */
int sub(int a, int b)
{
	return (a - b);
}

